# Portfolio

Personal portfolio site built with SvelteKit, Tailwind CSS, and daisyUI.

## Developing

Install dependencies and start the dev server:

```bash
npm install
npm run dev
```

## Building

```bash
npm run build
```

Preview the production build with `npm run preview`.

## Deploying

The site deploys to GitHub Pages via:

```bash
npm run deploy
```
